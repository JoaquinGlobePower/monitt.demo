# Clerk — Publishable key (PÚBLICA: viaja en el bundle del frontend, es segura de commitear).
# Se versiona para que el build de Cloudflare Pages / cualquier CI SIEMPRE la tenga
# (Vite hornea import.meta.env.VITE_* en tiempo de build).
# La SECRET key NO va aquí — vive solo en .env.local (gitignored). .env.local sobreescribe esto en local.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_cGVyZmVjdC1raXdpLTM2LmNsZXJrLmFjY291bnRzLmRldiQ
